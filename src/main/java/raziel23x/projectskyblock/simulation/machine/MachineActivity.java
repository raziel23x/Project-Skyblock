package raziel23x.projectskyblock.simulation.machine;

/** Observable backend lifecycle of a machine participant. */
public enum MachineActivity {
    SLEEPING,
    RUNNING,
    BLOCKED,
    INVALID
}
