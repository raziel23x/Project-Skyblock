package raziel23x.projectskyblock.simulation.machine.component;

/** Identifies the boundary that owns a restricted inventory view. */
public enum MachineInventoryViewKind {
    AUTOMATION,
    MENU
}
