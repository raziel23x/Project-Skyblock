package raziel23x.projectskyblock.simulation.machine.component;

/** Lifecycle state for one reusable machine processing component. */
public enum MachineProcessingStatus {
    IDLE,
    RUNNING,
    BLOCKED,
    READY_TO_COMPLETE
}
